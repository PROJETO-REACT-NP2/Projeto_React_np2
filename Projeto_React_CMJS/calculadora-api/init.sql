-- init.sql
-- Este script é executado automaticamente pelo Docker na primeira vez que o banco sobe.
-- Ele garante que o esquema principal da aplicação seja criado automaticamente.

CREATE SCHEMA IF NOT EXISTS calculadora_schema;
